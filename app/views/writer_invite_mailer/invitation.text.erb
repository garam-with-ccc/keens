You're invited to <%= @camp.name %>
=====================================

Hi<%= " #{@user.name}" if @user.name.present? %>,

<%= @invited_by.display_name %> invited you to <%= @camp.name %>
(<%= @camp.start_date.strftime("%b %-d") %> – <%= @camp.end_date.strftime("%b %-d, %Y") %>) on Keens.

Use the link below to sign in and see your schedule. It is good for one use and
expires on <%= @expires_at.strftime("%b %-d, %Y") %>.

<%= @accept_url %>

If you weren't expecting this, you can ignore the email.
