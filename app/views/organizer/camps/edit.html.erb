<% content_for :title, "Edit camp" %>

<section class="w-full max-w-2xl mx-auto">
  <h1 class="text-3xl font-semibold tracking-tight mb-2">Edit camp</h1>
  <p class="text-neutral-400 mb-8"><%= @camp.name %></p>
  <%= render "form", camp: @camp %>
</section>
